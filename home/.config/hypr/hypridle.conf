general {
    # No lock commands - just screen power management
}

listener {
    timeout = 1800                        # 30 minutes (1800 seconds)
    on-timeout = hyprctl dispatch dpms off   # turn off screen after 30 min
    on-resume = hyprctl dispatch dpms on     # turn screen back on immediately when input detected
}
